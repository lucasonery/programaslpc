#include <stdio.h>
 
int main(){
    
    int soma, a, b;
    
    scanf("%d %d", &a, &b);  //lê as entradas
    soma = a + b;   //soma as variáveis
    
    printf("SOMA = %d\n", soma); //imprime a resposta
    
    return 0;
}
