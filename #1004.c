#include <stdio.h>
 
int main(){
    
    int multi, a, b; // variáveis
    
    scanf("%d %d", &a, &b);  //lê as entradas
    multi = a * b;   //multiplica as variáveis
    
    printf("PROD = %d\n", multi); //imprime a resposta
    
    return 0;
}
